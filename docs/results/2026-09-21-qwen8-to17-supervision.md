# Qwen8对1.7B能力验收巡检

## 范围与时间

**最终状态：21:40全部完成，无训练。22:10恢复巡检时已确认退出与GPU空闲。**
分项成绩见[最终报告](2026-09-21-qwen8-to17-final.md)。以下保留现场观察的原始时间线。

2026-09-21北京时间19:47开始只读巡检ml2现有后台队列。
运行根目录为 `runs/20260921v1_qwen8_to17_diagnostics_ml2`，
控制器PID1872503，运行代码仍为不可变 `ce653fbe5a9aebceadd37d6f8cfd33e46a1bd11d`。
未重启任务、调整采样、修改模型或启动训练。后续进展必须查询LIVE状态，不能把本文快照当作实时状态。

## 已完成的学生单元

19:48:06，Qwen3-1.7B-Base完成vLLM初始化并开始独立解题。
19:48:56为70/128条，19:49:54为126/128条；尾部两条达到16K长度上限。
19:54:05队列已自动切换至8B-Base教师独立解题任务，学生单元完成文件已封存。

| 指标 | 学生独立解题结果 |
| --- | --- |
| 问题/轨迹 | 64题，每题2条，共128条 |
| 正确 | 13/128，10.15625% |
| 达到长度上限 | 2/128，1.5625% |
| 周期重复 | 1/128，0.78125% |
| 无有效boxed | 8/128，6.25% |
| 新生成think标签 | 0/128 |
| 平均输出长度 | 约976 token |
| 输出长度范围 | 106至16384 token |

这是固定DAPO诊断子集上的原始学生结果，不是benchmark完整评测，不代表教师验收结果。
生成先完成的短样本与最后完成的长样本分布不同，未完成阶段的截断率不可当作最终值。

## 工程与输出核查

- 128条原始轨迹和128条评分记录对应，64道题全部各有2个sample，无重复请求ID。
- `manifest.json`、`requests.json`、`raw.jsonl`、`results.jsonl`、`engine_events.jsonl`
  的SHA256均与封存清单一致；请求集合与原始记录集合完全相同。
- 实际模型为官方ModelScope Qwen/Qwen3-1.7B-Base，revision
  `b0786a09cd6ee101cd8c90e30a5727beb8230544`，并非历史训练checkpoint。
- 实际提示为题目、数学boxed要求和 `Solution:`，没有ChatML；EOS151643，
  温度1、top_p0.9、top_k=-1、每条16384token上限，seed按冻结题目/sample派生。
- vLLM0.11.0、torch2.8.0、transformers4.57.6、tokenizers0.22.2与冻结协议一致。
- 学生推理使用GPU0，显存约48687MiB、利用率约40%-41%；其他GPU空闲。
  CPU/GPU持续活动，无OOM或traceback。模型加载与文件校验期间GPU空闲属正常阶段切换。
- 抽查无有效boxed的记录，包含正常EOS结束但未输出框选答案的情况；未补写答案或修改grader。
- 长度截断和周期重复属于模型输出问题，保留原始证据，不当作需要自动重试的工程故障。

## 后续阶段

19:54:05教师独立解题子任务PID1876616启动，19:54:57仍在GPU加载前的文件校验阶段。
接下来依次为8B-Base独立解题、同学生前缀的师生续写、Base汇总，然后指令组的资产准备、
GPU非thinking检查和能力诊断。Base能力不通过不会跳过指令组；工程完整性失败则停队列。
截至此快照教师没有新增成绩，指令组尚未运行，任何组都不会自动启动训练。

## 后续已核实的进展

19:56:45教师五个分片加载完毕；19:57:42教师独立生成96/128条，轨迹与评分同步增长。
师生独立请求文件完全一致，包含题目、输入token、seed和采样参数；运行版本与协议也一致。
这一轮前台巡检随后被用户中断，远端后台队列继续运行，不声称中断期间有人持续盯守。

22:10用户要求继续时，读取LIVE状态确认队列已于21:40:04结束：
Base为inconclusive，指令组passed，training_started=false，控制器退出，四张GPU空闲。
未重新启动任何已完成阶段，未把教师能力通过解读为新训练授权。
随后在实际verl环境完成10个单元的只读审计，768条诊断和8条检查记录的封存哈希、
请求对应关系及历史grader重评分全部通过，两组验收决策重新计算一致。
审计产物为 `diagnostics/20260921_qwen17_pair_postrun_audit/report.json`，未改写原始运行产物。
