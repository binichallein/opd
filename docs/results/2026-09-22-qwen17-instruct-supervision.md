# Qwen1.7 指令版 Block3 正式训练巡检

## 00:48北京时间快照

仅只读检查ml2，不重启、不修改参数、不修改冻结部署。
控制器1896918仍为PPID1；正式Block3子任务1920585于00:07:30自动启动。
运行根目录 `runs/20260921v1_qwen17_instruct_blockfirst_seed21_ml2`，
冻结代码 `be736b5fac4f26ff59f4e2c21abafc456c2503f6`。

## 恢复与配置

- 两步GPU预检已完成，checkpoint/rollout/四rank优化器及`resume_gate.json`均passed。
- 恢复审计检查四rank RNG、调度器和数据状态，不宣称vLLM逐bit回放一致。
- 正式训练独立从官方Qwen3-1.7B初始化，教师Qwen3-8B，resume_mode=disable，seed21。
- 保存点仍为50/100/150/200。未到50步，因此正式checkpoint目录为空是预期行为。

## 已完成Step1至23

逐步审计736条实际轨迹：hash、历史DAPO输入顺序、请求seed、原生提示、
mask/logprob/EOS与停止原因均通过；23份位置诊断NPZ及连续标量记录已保存。

| 指标 | 结果 |
|---|---|
| 累计截断 | 6/736，0.8152% |
| thinking标签 | 0/736 |
| 周期重复检测 | 1/736，0.1359% |
| 最近5步截断 | 0/160 |
| 裁剪前梯度范数 | 最小0.8912、最大5.1924、最新1.3379 |
| 学生熵 | 最新0.5578，范围0.1888至0.5845 |
| 教师熵 | 最新0.4662 |
| Top16 overlap | 首步0.7272，最新0.7617 |
| SignFlipRate | 首步0.1722，最新0.2639 |
| WeightedSignFlipRate | 首步0.0623，最新0.1066 |
| NormalizedLeakage | 最新1.0056，范围0.9904至1.0417 |
| 非有限数值/overflow/underflow计数 | 全部为0 |
| 正式训练日志致命错误扫描 | 无OOM、Traceback或NCCL错误命中 |

截断集中在Step4(1条)、7(2条)、17(3条)。重复样本是Step17 sample_index17，
实际finish_reason=length，输出长度16384，尾部持续输出数字0，周期1匹配率100%。
这是实在的局部生成退化，不能因为梯度有限就忽略；但当前证据不支持全局collapse。

熵、输出长度和符号翻转率随题目与训练变化，当前为变化中的on-policy题目，
不是固定探针上的因果比较。不能仅凭其升降宣称方法有效或失效。

近10步平均87.408秒，据此剩余177步约4.30小时，约05:06完成训练，
不含checkpoint审计、绘图与完整评测；16K长输出可能改变ETA。

## 后续队列

Block3训练和验收后自动评测200/150/100/50，再评测初始学生；
之后独立Token恢复预检、200步训练和四权重完整评测。
本次没有增设自动停止阈值，也没有修改loss、采样、学习率或数据。

## 00:51进展确认

训练推进到Step24，新增32条轨迹单独审计通过，截断/think/周期重复均为0。
累计已审计768条，截断6条(0.78125%)，周期重复1条(0.13021%)，thinking标签0条。
Step24梯度范数1.099，学生/教师熵约0.481/0.399，位置诊断仍逐步保存。
四卡在后续计算中利用率均约99%，未改动训练任务。
