\paragraph{Reasoning transfer across teacher--student pairs.}
Table~\ref{tab:historyendpoints} illustrates two historical settings with positive Step200 Avg@8 differences. With Qwen3-1.7B-Base and the public GRPO teacher from Rethinking OPD \citep{rethinking}, Block3 improves Avg@8 on all four benchmarks: MATH500 rises from 45.85 to 54.75, AIME24 from 5.42 to 8.75, AIME25 from 2.50 to 5.42, and AMC23 from 21.84 to 27.26. These observations motivate local feedback sharing. DeepSeek/JustRL 1.5B also has positive endpoint differences, smaller on MATH500 and AMC23 than on AIME.

These are illustrative positive cases, not an exhaustive cross-model comparison. Other historical pairs favor Token; Table~\ref{tab:historyendpointsfull} in Appendix~\ref{app:history} retains all four endpoint comparisons and their protocol context, followed by every checkpoint result.

\begin{table}[htbp]
\centering\small
\caption{Selected positive historical Step200 Avg@8 examples (\%). Compare methods only within each student stratum. The complete comparison, including negative outcomes, appears in Table~\ref{tab:historyendpointsfull}.}
\label{tab:historyendpoints}
\input{generated/history_highlights}
\end{table}

\paragraph{Official Qwen3-8B to 1.7B Instruct.}
Table~\ref{tab:qwen17endpoint} tests both methods under native non-thinking chat with matched training and evaluation inputs. Relative to Token, Block3's Step200 Avg@8 differences are $+0.10$, $+2.50$, $+0.83$ and $+0.30$ points on MATH500, AIME24, AIME25 and AMC23. Both methods exceed the original student's four Avg@8 scores. All pointwise 95\% paired-question intervals for the method differences include zero, so the positive estimates do not resolve a statistical advantage at this endpoint.

Pass@8 separates average success from question coverage. Its Block3-minus-Token differences are $+0.20$, $-6.67$, $0.00$ and $-1.20$ points, and both methods reduce the original student's MATH500 coverage from 91.20\%. Thus higher mean accuracy can coexist with lower eight-attempt coverage. Appendix~\ref{app:instruct} reports the original student, all eight trained checkpoints and the associated behavior metrics.
\input{\qweninstructresults/qwen17_step200_en}

\paragraph{Qwen3-4B Base with the public GRPO teacher.}
The matched completion-prompt comparison gives a different endpoint pattern (Table~\ref{tab:qwen4endpoint}). At Step200, Block3's Avg@8 differences are $-0.52$, $-2.92$, $+0.42$ and $-3.01$ points. Token is higher on three tasks, while all pointwise 95\% paired-question intervals cross zero; neither superiority nor equivalence is established. On AIME24, Block3 has lower Avg@8 but solves one more question at least once, giving Pass@8 of 33.33\% versus 30.00\%. Together, the paired results show that credit granularity affects the learning trajectory, with benefits depending on the teacher--student setting and the metric.
\input{\qwenresults/qwen4_step200_en}

\IfFileExists{\qwenfigures/qwen4_accuracy.pdf}{%
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{\qwenfigures/qwen4_accuracy.pdf}
\caption{Current matched 4B comparison at all four saved checkpoints. Avg@8 is reported separately for each benchmark, with task-specific vertical ranges shared by both methods. Lines connect measured checkpoints, not additional evaluations. One training seed per method.}
\label{fig:qwen4accuracy}
\end{figure}
}{}
